version: '3'
services:
  dhcpd:
    restart: always
    image: lnls/dhcpd
    container_name: dhcpd
    command: ["enp2s0"]
    volumes:
      - ./data:/data
    network_mode: "host"
    dns: 10.128.255.6
    dns_search: lnls-sirius.com.br
